name: CI

# This repository is public while stromy-org is private. GitHub does not allow
# a public caller to load a reusable workflow from a private repository, so the
# small Python gate is intentionally self-contained here.

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
<<<<<<< before updating
  quality:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: astral-sh/setup-uv@v7
        with:
          version: "latest"
      - run: uv python install 3.13
      - run: uv sync
      - name: Lint
        run: uv run ruff check
      - name: Type check
        run: uv run pyright src/stromy_workflows_mcp
      - name: Workflow entitlements
        run: uv run python scripts/check_entitlements.py

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: astral-sh/setup-uv@v7
        with:
          version: "latest"
      - run: uv python install 3.13
      - run: uv sync
      - name: Tests
        run: uv run pytest
=======
  python-ci:
    # ci-dedup (inside the reusable workflow) reads the merged PR behind a push
    # and that PR's workflow runs. The org default token grants contents+packages
    # only, so without these the action 403s on every push and silently never
    # skips — the checks then run twice per change, which is the cost it exists to
    # remove. A called workflow inherits this job's grant and can only downgrade
    # it, so it has to be declared here rather than in ci-python.yml.
    permissions:
      contents: read
      packages: read
      pull-requests: read
      actions: read
    uses: stromy-org/stromy-org/.github/workflows/ci-python.yml@main
    with:
      source_package: "src/stromy_workflows_mcp"
    # Forward the org stromy-ci App secrets (CI_APP_ID / CI_APP_PRIVATE_KEY)
    # so the reusable workflow can clone private deps. No-op if absent.
    secrets: inherit
>>>>>>> after updating
