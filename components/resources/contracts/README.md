# Workflow contracts

Generated read-only bundle consumed by the facade. Stromy owns the contracts at
`stromy/workflows/<workflow>/config/contract.json`; regenerate from a stromy-org
checkout with:

```bash
python3 scripts/sync_contracts.py --source-root ../../Stromy
```

Do not author contract changes here.
